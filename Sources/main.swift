import Cocoa

@main
struct DisclosureGroupApp: App {
    var body: some Scene {
        Window("DisclosureGroup", id: "main") {
            ContentView()
        }
        .defaultSize(width: 400, height: 300)
    }
}